# Урок 1: Зачем нужен RAG?

## Введение

Представьте: вы запустили чат-бота для поддержки клиентов на базе GPT-4. Первые отзывы восторженные — бот отвечает связно и грамотно. Но через неделю начинаются жалобы: "Бот сказал, что наш новый продукт стоит 500 рублей, хотя цена 1500!" или "Он утверждает, что у нас есть филиал в Казани, которого нет уже два года".

Что произошло? LLM **галлюцинирует** — уверенно генерирует неправдивую информацию. И это не баг, а фундаментальное ограничение технологии. В этом уроке мы разберём, почему это происходит и как технология **RAG** (Retrieval-Augmented Generation) решает эту проблему.

## Цели урока

После завершения урока вы сможете:

- ✅ Понимать ключевые ограничения LLM
- ✅ Объяснить концепцию RAG простыми словами
- ✅ Продемонстрировать разницу между ответами с RAG и без
- ✅ Определить сценарии, где RAG необходим

## Ключевые термины

- **Cutoff date** — дата, до которой модель "знает" информацию из обучающих данных
- **Галлюцинация** — уверенная генерация фактически неверной информации
- **RAG (Retrieval-Augmented Generation)** — технология дополнения LLM внешними знаниями
- **Retriever** — компонент, находящий релевантные документы
- **Generator** — LLM, генерирующая ответ на основе найденного контекста

## 1. Ограничения языковых моделей

### Проблема 1: Дата обрезки знаний (Cutoff Date)

LLM обучаются на огромных текстовых корпусах, но эти данные имеют **конечную дату**. Всё, что произошло после — для модели не существует.

**Примеры cutoff dates:**
- GPT-3.5: сентябрь 2021
- GPT-4: апрель 2023
- Claude 3: март 2024

Это значит, что GPT-3.5 **не знает**:
- О событиях после сентября 2021
- О новых продуктах компании
- Об обновлениях в вашей документации

### Проблема 2: Галлюцинации

Галлюцинации — это когда модель **уверенно выдумывает** информацию. Это не ошибка в коде, а особенность архитектуры: модель обучена генерировать правдоподобный текст, а не проверять факты.

### Демонстрация: Поймаем галлюцинацию

```python
"""
Демонстрация галлюцинаций LLM.
Модель будет уверенно отвечать на вопросы о несуществующих фактах.
"""

import os
import requests
from dotenv import load_dotenv

load_dotenv()

def ask_llm(question: str, model: str = "openai/gpt-3.5-turbo") -> str:
    """Отправляет вопрос к LLM и возвращает ответ"""
    
    api_key = os.getenv("OPENROUTER_API_KEY")
    if not api_key:
        return "Ошибка: API ключ не найден"
    
    response = requests.post(
        "https://openrouter.ai/api/v1/chat/completions",
        headers={
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json"
        },
        json={
            "model": model,
            "messages": [
                {
                    "role": "system",
                    "content": "Ты — полезный ассистент. Отвечай кратко и по существу."
                },
                {
                    "role": "user",
                    "content": question
                }
            ],
            "max_tokens": 300,
            "temperature": 0.7
        }
    )
    
    if response.status_code == 200:
        return response.json()["choices"][0]["message"]["content"]
    else:
        return f"Ошибка API: {response.status_code}"


# Тест 1: Вопрос о событии после cutoff date
print("="*60)
print("ТЕСТ 1: Вопрос о недавних событиях")
print("="*60)

question1 = "Какие основные функции добавили в Python 3.13?"
answer1 = ask_llm(question1)

print(f"Вопрос: {question1}")
print(f"Ответ модели: {answer1}")
print()
print("⚠️ Обратите внимание: модель может выдумать функции,")
print("   которых нет в Python 3.13, или отказаться отвечать.")


# Тест 2: Вопрос о несуществующей компании
print("\n" + "="*60)
print("ТЕСТ 2: Вопрос о выдуманной компании")
print("="*60)

question2 = "Расскажи о компании TechnoVortex Inc и их продукте CloudMind."
answer2 = ask_llm(question2)

print(f"Вопрос: {question2}")
print(f"Ответ модели: {answer2}")
print()
print("⚠️ Компании TechnoVortex не существует!")
print("   Но модель может уверенно 'рассказать' о ней.")


# Тест 3: Вопрос с неверной предпосылкой
print("\n" + "="*60)
print("ТЕСТ 3: Вопрос с ложной предпосылкой")
print("="*60)

question3 = "Почему Python удалил поддержку классов в версии 4.0?"
answer3 = ask_llm(question3)

print(f"Вопрос: {question3}")
print(f"Ответ модели: {answer3}")
print()
print("⚠️ Python 4.0 не существует, и классы никто не удалял!")
print("   Качественная модель должна это указать.")
```

### 🔍 Проверьте себя

Запустите код выше и посмотрите на ответы. Задайте себе вопросы:
- Насколько уверенно модель отвечает на вопросы о несуществующих вещах?
- Признаёт ли она свою неуверенность?
- Можно ли отличить правду от выдумки без внешней проверки?

<details>
<summary>Типичные наблюдения</summary>

1. **Тест 1**: Модель либо признает незнание, либо выдумает функции
2. **Тест 2**: Часто модель создаёт правдоподобное описание несуществующей компании
3. **Тест 3**: Хорошая модель укажет на ошибку, но некоторые "подыграют"

Главный вывод: **нельзя доверять LLM без проверки фактов**!
</details>

### Проблема 3: Нет доступа к частным данным

LLM обучены на публичных данных интернета. Они **не знают**:
- Внутреннюю документацию вашей компании
- Данные из корпоративных систем
- Информацию о ваших продуктах и услугах
- Персональные данные клиентов

Это критично для бизнес-приложений:
- Чат-бот поддержки не знает вашу базу знаний
- Помощник юриста не знает внутренние документы
- Образовательный бот не знает ваши учебные материалы

## 2. Что такое RAG?

### Расшифровка

**RAG = Retrieval-Augmented Generation**
- **Retrieval** (извлечение) — поиск релевантных документов
- **Augmented** (дополненная) — модель получает дополнительный контекст
- **Generation** (генерация) — создание ответа на основе контекста

### Аналогия: Студент на экзамене

Представьте двух студентов на экзамене:

| Обычный LLM | LLM с RAG |
|-------------|-----------|
| Экзамен по памяти | Экзамен с открытой книгой |
| Помнит то, что учил | Может посмотреть в справочник |
| Может выдумать, если забыл | Отвечает на основе источника |
| Знания устаревают | Источники можно обновлять |

RAG превращает LLM из "студента с хорошей памятью" в "студента с доступом к справочникам".

### Архитектура RAG

```
┌─────────────────────────────────────────────────────────────┐
│                      Архитектура RAG                         │
└─────────────────────────────────────────────────────────────┘

     ❓ Вопрос пользователя
           │
           ▼
    ┌──────────────┐
    │  RETRIEVER   │  ← Поисковый компонент
    │  (поиск)     │
    └──────┬───────┘
           │
           │ Запрос к базе знаний
           ▼
    ┌──────────────┐
    │  📚 База     │  ← Ваши документы
    │   знаний     │
    └──────┬───────┘
           │
           │ Релевантные документы
           ▼
    ┌──────────────┐
    │  GENERATOR   │  ← LLM (GPT, Claude, etc.)
    │  (генерация) │
    └──────┬───────┘
           │
           │ Вопрос + Документы → Ответ
           ▼
     💬 Ответ с источниками
```

### Два ключевых компонента

**1. Retriever (Извлекатель)**
- Принимает вопрос пользователя
- Ищет релевантные документы в базе знаний
- Возвращает top-K наиболее подходящих фрагментов
- Использует семантический поиск (по смыслу, не по словам)

**2. Generator (Генератор)**
- Получает вопрос + найденные документы
- Формирует ответ на основе контекста
- Может цитировать источники
- Не выдумывает, если информации нет

## 3. Демонстрация: RAG vs обычный LLM

Давайте увидим разницу на практике. Создадим простейшую симуляцию RAG:

```python
"""
Демонстрация разницы между обычным LLM и LLM с контекстом (симуляция RAG).
"""

import os
import requests
from dotenv import load_dotenv

load_dotenv()


def ask_without_context(question: str) -> str:
    """Запрос к LLM БЕЗ дополнительного контекста"""
    
    api_key = os.getenv("OPENROUTER_API_KEY")
    
    response = requests.post(
        "https://openrouter.ai/api/v1/chat/completions",
        headers={
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json"
        },
        json={
            "model": "openai/gpt-3.5-turbo",
            "messages": [
                {
                    "role": "system",
                    "content": "Ты — полезный ассистент. Отвечай кратко."
                },
                {
                    "role": "user",
                    "content": question
                }
            ],
            "max_tokens": 300
        }
    )
    
    return response.json()["choices"][0]["message"]["content"]


def ask_with_context(question: str, context: str) -> str:
    """Запрос к LLM С дополнительным контекстом (симуляция RAG)"""
    
    api_key = os.getenv("OPENROUTER_API_KEY")
    
    # Формируем промпт в стиле RAG
    rag_prompt = f"""Используй ТОЛЬКО информацию из предоставленного контекста для ответа.
Если в контексте нет ответа на вопрос, честно скажи об этом.

КОНТЕКСТ:
{context}

ВОПРОС: {question}

ОТВЕТ (на основе контекста):"""
    
    response = requests.post(
        "https://openrouter.ai/api/v1/chat/completions",
        headers={
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json"
        },
        json={
            "model": "openai/gpt-3.5-turbo",
            "messages": [
                {
                    "role": "system",
                    "content": "Ты — ассистент, который отвечает ТОЛЬКО на основе предоставленного контекста. Не используй внешние знания."
                },
                {
                    "role": "user",
                    "content": rag_prompt
                }
            ],
            "max_tokens": 300
        }
    )
    
    return response.json()["choices"][0]["message"]["content"]


# ═══════════════════════════════════════════════════════════
# ТЕСТ: Вопрос о вымышленной компании
# ═══════════════════════════════════════════════════════════

# Наш "внутренний документ" с информацией о компании
company_document = """
ИНФОРМАЦИЯ О КОМПАНИИ "СОЛНЕЧНЫЙ СВЕТ"

Компания "Солнечный свет" основана в 2019 году в Новосибирске.
Основное направление: производство светодиодных ламп.

ПРОДУКТЫ:
- SunLamp Pro: профессиональная лампа для офисов, цена 2500 рублей
- SunLamp Home: домашняя лампа, цена 990 рублей
- SunLamp Mini: компактная лампа, цена 450 рублей

КОНТАКТЫ:
- Телефон: +7-383-123-45-67
- Email: info@solnechny-svet.ru
- Адрес: Новосибирск, ул. Ленина, 42

РЕЖИМ РАБОТЫ:
Пн-Пт: 9:00-18:00
Сб-Вс: выходной

АКЦИИ:
При заказе от 10 ламп — скидка 15%.
"""

question = "Сколько стоит лампа SunLamp Pro от компании 'Солнечный свет'?"

print("="*70)
print("СРАВНЕНИЕ: Обычный LLM vs LLM с контекстом (RAG)")
print("="*70)
print(f"\nВопрос: {question}")

# Ответ БЕЗ контекста
print("\n" + "-"*70)
print("❌ ОТВЕТ БЕЗ КОНТЕКСТА (обычный LLM):")
print("-"*70)
answer_no_context = ask_without_context(question)
print(answer_no_context)

# Ответ С контекстом
print("\n" + "-"*70)
print("✅ ОТВЕТ С КОНТЕКСТОМ (симуляция RAG):")
print("-"*70)
answer_with_context = ask_with_context(question, company_document)
print(answer_with_context)

print("\n" + "="*70)
print("АНАЛИЗ РЕЗУЛЬТАТОВ:")
print("="*70)
print("""
Без контекста: Модель либо признаёт, что не знает компанию,
               либо может выдумать информацию.

С контекстом:  Модель находит точную цену из документа (2500 рублей)
               и отвечает на основе реальных данных.

Это и есть суть RAG — предоставление модели релевантного контекста!
""")


# ═══════════════════════════════════════════════════════════
# ТЕСТ 2: Вопрос, ответа на который нет в контексте
# ═══════════════════════════════════════════════════════════

print("\n" + "="*70)
print("ТЕСТ 2: Вопрос без ответа в контексте")
print("="*70)

question2 = "Какая гарантия на лампы SunLamp?"

print(f"\nВопрос: {question2}")
print("(В документе НЕТ информации о гарантии)")

print("\n" + "-"*70)
print("Ответ с RAG-подходом:")
print("-"*70)
answer2 = ask_with_context(question2, company_document)
print(answer2)

print("\n📝 Хорошая RAG-система должна честно сказать,")
print("   что информации о гарантии нет в документах.")
```

### 🔍 Проверьте себя

Прежде чем запускать код, попробуйте предсказать:
1. Что ответит модель без контекста на вопрос о цене?
2. Как изменится ответ с контекстом?
3. Что ответит модель на вопрос о гарантии (которой нет в документе)?

<details>
<summary>Ожидаемые результаты</summary>

1. **Без контекста**: Модель скажет, что не знает такую компанию, или выдумает цену
2. **С контекстом**: Точный ответ — 2500 рублей
3. **О гарантии**: Качественная система скажет, что информации нет в документах
</details>

## 4. Компоненты архитектуры RAG (детально)

Теперь разберём, из чего состоит полноценная RAG-система:

### Этап 1: Подготовка базы знаний (индексация)

```
📄 Документы (PDF, TXT, HTML...)
        │
        ▼
   ✂️ Chunking
   (нарезка на фрагменты 200-500 токенов)
        │
        ▼
   🔢 Embedding Model
   (текст → вектор чисел)
        │
        ▼
   🗄️ Vector Database
   (хранение и индексация)
```

**Chunking (нарезка)**
- Документы слишком длинные для эмбеддинга целиком
- Разбиваем на фрагменты по 200-500 токенов
- Сохраняем overlap (перекрытие) для контекста

**Embedding (эмбеддинг)**
- Каждый фрагмент превращается в вектор чисел
- Похожие по смыслу тексты → близкие векторы
- Размерность: обычно 384-1536

**Vector Database (векторная база)**
- Специализированное хранилище для векторов
- Быстрый поиск похожих (nearest neighbors)
- Примеры: FAISS, Chroma, Pinecone

### Этап 2: Обработка запроса

```
❓ Вопрос пользователя
        │
        ▼
   🔢 Embedding Model
   (вопрос → вектор)
        │
        ▼
   🔍 Semantic Search
   (поиск близких векторов в базе)
        │
        ▼
   📚 Top-K документов
   (наиболее релевантные фрагменты)
        │
        ▼
   📝 Формирование промпта
   (вопрос + контекст из документов)
        │
        ▼
   🤖 LLM
   (генерация ответа)
        │
        ▼
   💬 Ответ с источниками
```

### Почему это работает?

| Компонент | Проблема без него | Решение с ним |
|-----------|-------------------|---------------|
| Chunking | Документ не помещается в контекст | Релевантные фрагменты подаются точечно |
| Embeddings | Поиск только по ключевым словам | Семантический поиск по смыслу |
| Vector DB | Медленный перебор всех документов | Быстрый поиск O(log n) |
| LLM | Нет обобщения и синтеза | Формирует связный ответ |

## 5. Сценарии применения RAG

### Где RAG критически необходим

**1. Корпоративные чат-боты**
- База знаний компании
- FAQ и инструкции
- Внутренняя документация

**2. Юридические помощники**
- Законодательство и нормативные акты
- Судебная практика
- Внутренние политики компании

**3. Медицинские консультанты**
- Протоколы лечения
- Инструкции к препаратам
- Медицинские справочники

**4. Образовательные системы**
- Учебники и методические материалы
- Конспекты лекций
- **Связь с модулем 3**: SchoolBot может использовать учебники через RAG!

**5. Техническая поддержка**
- Документация продуктов
- Troubleshooting guides
- История обращений

### Когда RAG НЕ поможет

- **Вопросы, требующие рассуждения**: "Что лучше: Python или Java?"
- **Творческие задачи**: "Напиши стихотворение"
- **Нет релевантных документов**: если ответа нет в базе, RAG не поможет
- **Математические вычисления**: RAG не калькулятор

## Практические задания

### 🟢 Базовый уровень

**Задание 1: Эксперименты с галлюцинациями**

Используя код из раздела 1, проведите эксперименты:

1. Задайте модели 5 вопросов о событиях 2024-2025 годов
2. Запишите ответы в таблицу:

| Вопрос | Ответ модели | Правда/Выдумка | Уверенность модели |
|--------|--------------|----------------|-------------------|
| ... | ... | ... | ... |

3. Сделайте выводы о надёжности LLM без RAG

**Задание 2: Сравнение с контекстом и без**

Используя код из раздела 3:

1. Создайте свой "документ" с информацией (о хобби, проекте, etc.)
2. Задайте 3 вопроса:
   - Один с ответом в документе
   - Один без ответа в документе
   - Один частично в документе
3. Сравните ответы с контекстом и без

### 🟡 Средний уровень

**Задание 3: Функция ask_with_context**

Создайте улучшенную функцию:

```python
def ask_with_context(
    question: str, 
    context: str,
    model: str = "openai/gpt-3.5-turbo",
    require_sources: bool = True
) -> dict:
    """
    Задаёт вопрос с контекстом и возвращает структурированный ответ.
    
    Returns:
        {
            "answer": str,           # Ответ на вопрос
            "has_answer": bool,      # Найден ли ответ в контексте
            "confidence": str,       # high/medium/low
            "relevant_quote": str    # Цитата из контекста
        }
    """
    # Ваша реализация
    pass
```

Протестируйте на 5 разных вопросах и контекстах.

**Задание 4: Анализ сценариев**

Для каждого из следующих сценариев определите:
- Нужен ли RAG?
- Какие документы войдут в базу знаний?
- Какие сложности могут возникнуть?

Сценарии:
1. Чат-бот для интернет-магазина электроники
2. Помощник по изучению истории России
3. Генератор креативных идей для маркетинга
4. Система ответов на вопросы по Python
5. Переводчик художественных текстов

### 🔴 Продвинутый уровень

**Задание 5: Поиск галлюцинаций**

1. Найдите 5 реальных примеров галлюцинаций LLM (можете генерировать сами)
2. Для каждой галлюцинации:
   - Опишите, что модель выдумала
   - Предложите контекст, который исправит ошибку
   - Проверьте гипотезу, добавив контекст
3. Оформите результаты в виде отчёта

**Задание 6: Проектирование RAG-системы**

Спроектируйте RAG-систему для SchoolBot из модуля 3:

1. Какие документы войдут в базу знаний? (учебники, задачники, справочники)
2. Как организовать chunking для разных типов контента?
3. Как система будет обрабатывать вопросы без ответа?
4. Нарисуйте схему архитектуры

## Контрольные вопросы

1. **Что означает аббревиатура RAG?**
   <details>
   <summary>Ответ</summary>
   Retrieval-Augmented Generation — генерация, дополненная извлечением информации
   </details>

2. **Почему LLM галлюцинируют?**
   <details>
   <summary>Ответ</summary>
   LLM обучены генерировать правдоподобный текст, а не проверять факты. Их знания ограничены датой обучения, и они не имеют доступа к внешним источникам для верификации.
   </details>

3. **Назовите два основных компонента RAG**
   <details>
   <summary>Ответ</summary>
   Retriever (находит релевантные документы в базе знаний) и Generator (LLM, генерирующая ответ на основе найденного контекста)
   </details>

4. **В каких случаях RAG не поможет?**
   <details>
   <summary>Ответ</summary>
   Когда нет релевантных документов в базе, для творческих задач, для математических вычислений, для вопросов, требующих рассуждения а не фактов
   </details>

5. **Чем RAG отличается от fine-tuning?**
   <details>
   <summary>Ответ</summary>
   RAG добавляет внешние данные на этапе inference (запроса) без изменения модели. Fine-tuning изменяет веса модели во время дополнительного обучения. RAG гибче и проще обновлять, fine-tuning глубже интегрирует знания.
   </details>

## Заключение урока

### Что мы изучили

В этом уроке мы разобрали:

- **Ограничения LLM**: cutoff date, галлюцинации, отсутствие доступа к частным данным
- **Концепция RAG**: Retriever находит документы, Generator создаёт ответ
- **Практическая демонстрация**: разница между ответами с контекстом и без
- **Сценарии применения**: корпоративные боты, юридические системы, образование

### Связь с предыдущими модулями

- **Модуль 2, Урок 3**: LLM-as-a-judge поможет оценивать качество RAG
- **Модуль 2, Урок 4**: Управление контекстом критично для RAG
- **Модуль 3**: SchoolBot может использовать учебники через RAG!

### Что дальше

В следующем уроке **"Эмбеддинги и векторные базы"** мы:
- Узнаем, как текст превращается в векторы
- Создадим первую векторную базу данных
- Реализуем семантический поиск

### Ваш прогресс

🎯 **Отличное начало!** Теперь вы понимаете:
- ✅ Почему LLM нельзя доверять без проверки
- ✅ Как RAG решает проблему галлюцинаций
- ✅ Какие компоненты нужны для RAG-системы

**Готовы погрузиться в векторные представления?** Переходите к [Уроку 2: Эмбеддинги и векторные базы](lesson_2_embeddings_vectors.md)!

---

## Дополнительные материалы

### Статьи:
- [RAG: Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401) — оригинальная статья Meta AI
- [A Survey on Retrieval-Augmented Generation](https://arxiv.org/abs/2312.10997) — обзор методов RAG

### Инструменты:
- [LangChain](https://python.langchain.com/) — фреймворк для RAG
- [LlamaIndex](https://www.llamaindex.ai/) — альтернативный фреймворк
- [Haystack](https://haystack.deepset.ai/) — NLP фреймворк с RAG

### Видео:
- [RAG Explained in 5 Minutes](https://www.youtube.com/results?search_query=rag+explained) — поиск обучающих видео

