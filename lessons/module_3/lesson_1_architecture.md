# Урок 1: Проектирование AI-помощника для школьников

## Введение

Добро пожаловать в практический модуль! За предыдущие два модуля вы изучили множество техник: работу с API, промпт-инжиниринг, роутинг моделей, LLM-as-a-judge, управление контекстом. Теперь пришло время объединить всё это в одном реальном проекте.

В этом уроке мы начнём создавать **SchoolBot** — умного помощника для школьников 9-11 классов, готовящихся к ОГЭ и ЕГЭ. Мы проанализируем требования, спроектируем архитектуру и создадим базовую структуру проекта.

**Почему именно помощник для школьников?**
- Понятная предметная область с чёткими требованиями
- Возможность использовать все изученные техники
- Практическая польза — проект можно реально использовать
- Интересные вызовы: адаптация под разные предметы и уровни

## Цели урока

После завершения урока вы сможете:

- ✅ Анализировать требования к LLM-приложению
- ✅ Проектировать архитектуру AI-системы с несколькими компонентами
- ✅ Определять функциональные режимы и интерфейс
- ✅ Создавать базовую структуру Python-проекта

## Ключевые термины

- **Архитектура приложения** — структура системы, описывающая компоненты и их взаимодействие
- **Модульная структура** — организация кода в независимые модули с чёткими интерфейсами
- **CLI (Command Line Interface)** — интерфейс командной строки для взаимодействия с программой
- **Фасад (Facade)** — паттерн проектирования, предоставляющий простой интерфейс к сложной системе

## 1. Анализ требований

### Кто наши пользователи?

**Школьники 9-11 классов**, которые:
- Готовятся к ОГЭ (9 класс) или ЕГЭ (10-11 классы)
- Хотят разобраться в сложных темах
- Нуждаются в практике с тестовыми заданиями
- Ценят простые и понятные объяснения с примерами из жизни

### Что должен уметь помощник?

Проанализируем типичный сценарий использования:

```
Школьник: "Объясни, что такое производная в математике"
SchoolBot: [Объясняет простым языком с примерами]

Школьник: "Дай мне несколько заданий на эту тему для подготовки к ЕГЭ"
SchoolBot: [Генерирует задания в формате ЕГЭ]

Школьник: "Ответ на первое задание: 5"
SchoolBot: [Проверяет ответ, объясняет ошибки если есть]

Школьник: "Сделай мне конспект по этой теме для повторения"
SchoolBot: [Создаёт краткий структурированный конспект]
```

### Функциональные требования

На основе анализа выделим основные режимы работы:

| Режим | Описание | Применение техник |
|-------|----------|-------------------|
| **Объяснение** | Разъяснение темы простым языком | Промпт-инжиниринг, адаптация под уровень |
| **Тестирование** | Генерация заданий ОГЭ/ЕГЭ | Few-shot, форматирование ответа |
| **Конспект** | Краткое изложение материала | Суммаризация, структурирование |
| **Репетитор** | Интерактивный диалог | Управление контекстом, CoT |

### Нефункциональные требования

- **Адаптивность**: настройка под класс (9/10/11) и предмет
- **Надёжность**: работа при недоступности основной модели (fallback)
- **Качество**: проверка объяснений на понятность (LLM-as-a-judge)
- **Сохранение прогресса**: история занятий между сессиями

### 🔍 Проверьте себя

Прежде чем продолжить, подумайте:
- Какие ещё функции могли бы быть полезны школьнику?
- Какие предметы требуют особого подхода в объяснениях?

<details>
<summary>Размышления</summary>

Дополнительные функции могут включать:
- Составление плана подготовки к экзамену
- Отслеживание слабых мест ученика
- Рекомендации по дополнительным материалам

Особый подход нужен для:
- Математика/физика — пошаговые решения с Chain-of-Thought
- Русский язык — работа с текстами и правилами
- История — хронология и причинно-следственные связи
</details>

## 2. Проектирование архитектуры

### Основные компоненты системы

Наше приложение будет состоять из следующих компонентов:

```
┌─────────────────────────────────────────────────────────────┐
│                      CLI Interface                           │
│                 (Интерфейс пользователя)                     │
└─────────────────────────┬───────────────────────────────────┘
                          │ команды
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    SchoolAssistant                           │
│              (Главный класс - "мозг" системы)                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ • explain_topic()  - объяснение темы                │    │
│  │ • generate_exam()  - создание теста                 │    │
│  │ • check_answer()   - проверка ответа                │    │
│  │ • create_summary() - конспект                       │    │
│  │ • tutor_mode()     - режим репетитора               │    │
│  └─────────────────────────────────────────────────────┘    │
└──────┬──────────────┬──────────────┬──────────────┬─────────┘
       │              │              │              │
       ▼              ▼              ▼              ▼
┌──────────────┐┌──────────────┐┌──────────────┐┌──────────────┐
│ ModelRouter  ││ ContextMgr   ││QualityChecker││ ExamQuiz     │
│              ││              ││              ││              │
│ Выбор модели ││ Управление   ││ Проверка     ││ Генерация    │
│ под задачу   ││ контекстом   ││ качества     ││ тестов       │
└──────────────┘└──────────────┘└──────────────┘└──────────────┘
       │              │              │              │
       └──────────────┴──────────────┴──────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    PromptLibrary                             │
│           (Шаблоны промптов для разных задач)                │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    OpenRouter API                            │
│              (Доступ к языковым моделям)                     │
└─────────────────────────────────────────────────────────────┘
```

### Описание компонентов

**1. CLI Interface (`main.py`)**
- Точка входа в приложение
- Обработка команд пользователя
- Отображение ответов

**2. SchoolAssistant (`assistant.py`)**
- Главный класс — "фасад" системы
- Координирует работу всех компонентов
- Предоставляет простой API для CLI

**3. ModelRouter (`router.py`)**
- Выбирает подходящую модель для задачи
- Простое объяснение → дешёвая модель
- Сложный анализ → мощная модель
- Реализует fallback при ошибках

**4. ContextManager (`context.py`)**
- Хранит историю диалога
- Применяет стратегии управления контекстом
- Резюмирует длинные сессии

**5. QualityChecker (`quality.py`)**
- LLM-as-a-judge для проверки качества
- Оценивает понятность объяснений
- Предлагает улучшения

**6. ExamQuizGenerator (`exam_quiz.py`)**
- Генерирует задания в формате ОГЭ/ЕГЭ
- Проверяет ответы ученика
- Отслеживает прогресс

**7. PromptLibrary (`prompts.py`)**
- Шаблоны промптов для разных задач
- Адаптация под предметы и классы
- Переиспользуемые конструкции

### Поток данных

Рассмотрим, как обрабатывается команда `/explain производная`:

```
1. CLI получает команду "/explain производная"
       │
       ▼
2. SchoolAssistant.explain_topic("производная")
       │
       ├─► ModelRouter выбирает модель (средняя для объяснения)
       │
       ├─► PromptLibrary формирует промпт с учётом:
       │   • Предмет: математика
       │   • Класс: 11
       │   • Уровень: средний
       │
       ├─► ContextManager добавляет контекст предыдущих вопросов
       │
       ▼
3. Запрос к OpenRouter API
       │
       ▼
4. QualityChecker проверяет качество объяснения
       │
       ├─► Если качество низкое → улучшить и повторить
       │
       ▼
5. ContextManager сохраняет объяснение в историю
       │
       ▼
6. CLI отображает результат пользователю
```

### 🔍 Проверьте себя

Посмотрите на архитектуру и ответьте:
- Какой компонент отвечает за выбор между GPT-4 и более дешёвой моделью?
- Где хранится история разговора?
- Что произойдёт, если основная модель недоступна?

<details>
<summary>Ответы</summary>

1. **ModelRouter** отвечает за выбор модели
2. **ContextManager** хранит историю разговора
3. **ModelRouter** переключится на резервную модель (fallback)
</details>

## 3. Создание структуры проекта

### Структура директорий

Создадим следующую структуру:

```
schoolbot/
├── __init__.py          # Делает директорию пакетом Python
├── main.py              # Точка входа, CLI-интерфейс
├── assistant.py         # Главный класс SchoolAssistant
├── router.py            # Роутер моделей
├── context.py           # Менеджер контекста
├── quality.py           # LLM-as-a-judge
├── exam_quiz.py         # Генератор тестов
├── prompts.py           # Библиотека промптов
├── config.py            # Конфигурация
sessions/                # Директория для сохранённых сессий
requirements.txt         # Зависимости
.env.example             # Пример файла с переменными окружения
README.md                # Документация проекта
```

### Создание файлов проекта

Начнём с создания базовой структуры. Создайте директорию проекта:

```bash
mkdir schoolbot
cd schoolbot
mkdir sessions
```

### Файл конфигурации (`config.py`)

```python
"""
Конфигурация SchoolBot — помощника для подготовки к экзаменам.
"""

import os
from dotenv import load_dotenv

# Загружаем переменные окружения из .env файла
load_dotenv()

# API настройки
OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
OPENROUTER_BASE_URL = "https://openrouter.ai/api/v1/chat/completions"

# Модели для разных задач
MODELS = {
    # Мощные модели для сложных задач
    "premium": [
        "openai/gpt-4-turbo",
        "anthropic/claude-3.5-sonnet",
    ],
    # Сбалансированные модели для большинства задач
    "balanced": [
        "meta-llama/llama-3-70b-instruct",
        "mistralai/mixtral-8x22b-instruct",
    ],
    # Быстрые модели для простых задач
    "fast": [
        "meta-llama/llama-3-8b-instruct",
        "google/gemma-7b-it",
    ]
}

# Модель по умолчанию
DEFAULT_MODEL = "meta-llama/llama-3-70b-instruct"

# Поддерживаемые предметы
SUBJECTS = {
    "математика": {
        "aliases": ["math", "алгебра", "геометрия"],
        "description": "Алгебра, геометрия, начала анализа"
    },
    "физика": {
        "aliases": ["physics", "физ"],
        "description": "Механика, термодинамика, электричество, оптика"
    },
    "информатика": {
        "aliases": ["informatics", "инфа", "программирование"],
        "description": "Программирование, алгоритмы, системы счисления"
    },
    "русский": {
        "aliases": ["russian", "рус"],
        "description": "Орфография, пунктуация, стилистика"
    },
    "история": {
        "aliases": ["history", "ист"],
        "description": "История России и мира"
    },
    "обществознание": {
        "aliases": ["social", "общество", "обществ"],
        "description": "Право, экономика, социология, политология"
    },
    "биология": {
        "aliases": ["biology", "био"],
        "description": "Ботаника, зоология, анатомия, генетика"
    },
    "химия": {
        "aliases": ["chemistry", "хим"],
        "description": "Органическая и неорганическая химия"
    },
    "английский": {
        "aliases": ["english", "англ"],
        "description": "Грамматика, лексика, чтение, письмо"
    }
}

# Уровни сложности
DIFFICULTY_LEVELS = {
    "easy": {
        "description": "Базовый уровень, простые объяснения",
        "grade_range": [9]
    },
    "medium": {
        "description": "Средний уровень, стандартные задания",
        "grade_range": [9, 10]
    },
    "hard": {
        "description": "Продвинутый уровень, сложные задания ЕГЭ",
        "grade_range": [10, 11]
    }
}

# Настройки контекста
CONTEXT_CONFIG = {
    "max_messages": 20,           # Максимум сообщений в контексте
    "summary_threshold": 15,      # Когда начинать резюмирование
    "keep_recent": 5              # Сколько последних сообщений сохранять при резюме
}

# Настройки качества
QUALITY_CONFIG = {
    "min_score": 7,               # Минимальная оценка качества (из 10)
    "max_improvements": 2,        # Максимум попыток улучшения
    "check_explanations": True    # Проверять ли качество объяснений
}

# Пути к файлам
SESSIONS_DIR = "sessions"
```

### Файл промптов (`prompts.py`)

```python
"""
Библиотека промптов для SchoolBot.
Содержит шаблоны для разных образовательных задач.
"""

class PromptLibrary:
    """Библиотека промптов для образовательного помощника"""
    
    # Системный промпт для всех запросов
    SYSTEM_BASE = """Ты — умный помощник для школьников старших классов (9-11 класс), 
которые готовятся к ОГЭ и ЕГЭ. 

Твои принципы работы:
1. Объясняй простым, понятным языком без лишнего академизма
2. Используй примеры из повседневной жизни, чтобы сложные концепции стали понятнее
3. Структурируй информацию: списки, пункты, выделения главного
4. Учитывай уровень ученика (класс и сложность)
5. Будь дружелюбным и поддерживающим — подготовка к экзаменам это стресс

Текущий предмет: {subject}
Класс ученика: {grade}
Уровень сложности: {difficulty}
"""

    # Промпт для объяснения темы
    EXPLAIN_TOPIC = """Объясни тему "{topic}" для ученика {grade} класса.

Требования к объяснению:
1. Начни с простого определения (1-2 предложения)
2. Объясни основную идею понятным языком
3. Приведи 2-3 примера из жизни или школьной практики
4. Покажи, где это применяется (в том числе в заданиях экзамена)
5. Подведи краткий итог (что важно запомнить)

Уровень сложности: {difficulty}
Если нужно, используй формулы, но обязательно объясняй каждый символ.
"""

    # Промпт для генерации заданий
    GENERATE_EXAM = """Создай {num_questions} тестовых заданий по теме "{topic}" 
в формате {exam_type} для {grade} класса.

Требования к заданиям:
1. Задания должны соответствовать формату реального экзамена {exam_type}
2. Включи разные типы: с кратким ответом, с выбором варианта, с развёрнутым ответом
3. Укажи правильный ответ для каждого задания (в скрытом виде)
4. Сложность должна соответствовать уровню: {difficulty}

Формат ответа:
---
ЗАДАНИЕ 1
[Текст задания]
Варианты (если есть): A) ... B) ... C) ... D) ...
ПРАВИЛЬНЫЙ ОТВЕТ: [ответ]
ПОЯСНЕНИЕ: [краткое пояснение]
---
"""

    # Промпт для проверки ответа
    CHECK_ANSWER = """Проверь ответ ученика на задание.

Задание: {question}
Правильный ответ: {correct_answer}
Ответ ученика: {student_answer}

Твоя задача:
1. Определи, правильный ли ответ (полностью, частично или неправильный)
2. Если ответ неправильный или частично правильный — объясни ошибку понятным языком
3. Покажи правильный ход решения
4. Дай совет, как избежать подобных ошибок

Формат ответа:
РЕЗУЛЬТАТ: [Правильно / Частично правильно / Неправильно]
КОММЕНТАРИЙ: [объяснение]
ПРАВИЛЬНОЕ РЕШЕНИЕ: [если нужно]
СОВЕТ: [рекомендация]
"""

    # Промпт для создания конспекта
    CREATE_SUMMARY = """Создай краткий конспект по теме "{topic}" для быстрого повторения 
перед экзаменом {exam_type}.

Требования к конспекту:
1. Уложись в 300-400 слов
2. Выдели ключевые понятия и определения
3. Включи основные формулы/правила (если есть)
4. Добавь мнемонические приёмы для запоминания
5. Укажи типичные ошибки, которых нужно избегать

Структура:
📌 ГЛАВНОЕ: [основная идея в 1-2 предложениях]
📝 КЛЮЧЕВЫЕ ПОНЯТИЯ: [список]
📐 ФОРМУЛЫ/ПРАВИЛА: [если применимо]
💡 КАК ЗАПОМНИТЬ: [мнемоники, ассоциации]
⚠️ ТИПИЧНЫЕ ОШИБКИ: [на что обратить внимание]
"""

    # Промпт для режима репетитора
    TUTOR_MODE = """Ты ведёшь урок по предмету "{subject}" с учеником {grade} класса.
Тема урока: {topic}

Веди диалог как опытный репетитор:
1. Задавай наводящие вопросы, чтобы ученик сам пришёл к пониманию
2. Если ученик ошибается, не давай сразу ответ — помоги найти ошибку
3. Хвали за правильные рассуждения
4. Разбивай сложные темы на простые шаги
5. Проверяй понимание перед переходом к следующему пункту

Последнее сообщение ученика: {user_message}

Контекст предыдущего разговора учтён в истории диалога.
"""

    # Промпт для оценки качества объяснения
    QUALITY_CHECK = """Оцени качество следующего объяснения для школьника {grade} класса.

Тема: {topic}
Объяснение:
---
{explanation}
---

Критерии оценки (каждый от 1 до 10):
1. ПОНЯТНОСТЬ: насколько просто и ясно изложен материал
2. ПОЛНОТА: все ли важные аспекты темы раскрыты
3. ПРИМЕРЫ: есть ли понятные примеры из жизни
4. СТРУКТУРА: логично ли организован материал
5. СООТВЕТСТВИЕ УРОВНЮ: подходит ли для ученика этого класса

Формат ответа:
```json
{{
    "понятность": [1-10],
    "полнота": [1-10],
    "примеры": [1-10],
    "структура": [1-10],
    "соответствие_уровню": [1-10],
    "общая_оценка": [1-10],
    "рекомендации": ["рекомендация 1", "рекомендация 2"]
}}
```
"""

    @classmethod
    def get_system_prompt(cls, subject: str, grade: int, difficulty: str) -> str:
        """Формирует системный промпт с параметрами"""
        return cls.SYSTEM_BASE.format(
            subject=subject,
            grade=grade,
            difficulty=difficulty
        )
    
    @classmethod
    def get_explain_prompt(cls, topic: str, grade: int, difficulty: str) -> str:
        """Формирует промпт для объяснения темы"""
        return cls.EXPLAIN_TOPIC.format(
            topic=topic,
            grade=grade,
            difficulty=difficulty
        )
    
    @classmethod
    def get_exam_prompt(cls, topic: str, num_questions: int, 
                        exam_type: str, grade: int, difficulty: str) -> str:
        """Формирует промпт для генерации заданий"""
        return cls.GENERATE_EXAM.format(
            topic=topic,
            num_questions=num_questions,
            exam_type=exam_type.upper(),
            grade=grade,
            difficulty=difficulty
        )
    
    @classmethod
    def get_check_answer_prompt(cls, question: str, 
                                 correct_answer: str, student_answer: str) -> str:
        """Формирует промпт для проверки ответа"""
        return cls.CHECK_ANSWER.format(
            question=question,
            correct_answer=correct_answer,
            student_answer=student_answer
        )
    
    @classmethod
    def get_summary_prompt(cls, topic: str, exam_type: str) -> str:
        """Формирует промпт для создания конспекта"""
        return cls.CREATE_SUMMARY.format(
            topic=topic,
            exam_type=exam_type.upper()
        )
    
    @classmethod
    def get_tutor_prompt(cls, subject: str, topic: str, 
                         grade: int, user_message: str) -> str:
        """Формирует промпт для режима репетитора"""
        return cls.TUTOR_MODE.format(
            subject=subject,
            topic=topic,
            grade=grade,
            user_message=user_message
        )
    
    @classmethod
    def get_quality_check_prompt(cls, topic: str, 
                                  explanation: str, grade: int) -> str:
        """Формирует промпт для проверки качества"""
        return cls.QUALITY_CHECK.format(
            topic=topic,
            explanation=explanation,
            grade=grade
        )
```

### Базовый класс помощника (`assistant.py`)

```python
"""
Главный класс SchoolAssistant — "мозг" нашего помощника.
В этом уроке создаём базовую структуру, которую будем расширять.
"""

import os
from typing import Dict, List, Optional
from config import (
    SUBJECTS, DIFFICULTY_LEVELS, DEFAULT_MODEL,
    CONTEXT_CONFIG, QUALITY_CONFIG
)
from prompts import PromptLibrary


class SchoolAssistant:
    """
    Умный помощник для школьников старших классов.
    Помогает готовиться к ОГЭ и ЕГЭ по разным предметам.
    """
    
    def __init__(self):
        """Инициализация помощника"""
        # Текущие настройки сессии
        self.current_subject = "математика"
        self.current_grade = 11
        self.current_difficulty = "medium"
        
        # Компоненты (будут реализованы в следующих уроках)
        self.router = None          # ModelRouter
        self.context_manager = None # ContextManager  
        self.quality_checker = None # QualityChecker
        self.exam_generator = None  # ExamQuizGenerator
        
        # История текущей сессии
        self.session_history: List[Dict] = []
        
        print("🎓 SchoolBot инициализирован!")
        print(f"   Предмет: {self.current_subject}")
        print(f"   Класс: {self.current_grade}")
        print(f"   Сложность: {self.current_difficulty}")
    
    # ==================== ОСНОВНЫЕ МЕТОДЫ ====================
    
    def explain_topic(self, topic: str) -> str:
        """
        Объясняет тему простым языком с примерами.
        
        Args:
            topic: Тема для объяснения
            
        Returns:
            Понятное объяснение темы
        """
        # TODO: Реализация в Уроке 2
        return f"[Заглушка] Объяснение темы: {topic}"
    
    def generate_exam(self, topic: str, exam_type: str = "ege", 
                      num_questions: int = 3) -> str:
        """
        Генерирует тестовые задания в формате ОГЭ/ЕГЭ.
        
        Args:
            topic: Тема для заданий
            exam_type: Тип экзамена ('oge' или 'ege')
            num_questions: Количество заданий
            
        Returns:
            Задания с ответами
        """
        # TODO: Реализация в Уроке 3
        return f"[Заглушка] {num_questions} заданий по теме: {topic}"
    
    def check_answer(self, question: str, student_answer: str) -> str:
        """
        Проверяет ответ ученика на задание.
        
        Args:
            question: Текст задания
            student_answer: Ответ ученика
            
        Returns:
            Результат проверки с пояснениями
        """
        # TODO: Реализация в Уроке 3
        return f"[Заглушка] Проверка ответа: {student_answer}"
    
    def create_summary(self, topic: str) -> str:
        """
        Создаёт краткий конспект по теме.
        
        Args:
            topic: Тема для конспекта
            
        Returns:
            Структурированный конспект
        """
        # TODO: Реализация в Уроке 2
        return f"[Заглушка] Конспект по теме: {topic}"
    
    def tutor_message(self, user_message: str, topic: str = None) -> str:
        """
        Обрабатывает сообщение в режиме репетитора.
        
        Args:
            user_message: Сообщение ученика
            topic: Текущая тема (опционально)
            
        Returns:
            Ответ репетитора
        """
        # TODO: Реализация в Уроке 2
        return f"[Заглушка] Ответ репетитора на: {user_message}"
    
    # ==================== НАСТРОЙКИ ====================
    
    def set_subject(self, subject: str) -> bool:
        """
        Устанавливает текущий предмет.
        
        Args:
            subject: Название предмета
            
        Returns:
            True если предмет найден и установлен
        """
        # Ищем предмет по названию или алиасу
        subject_lower = subject.lower()
        
        for subj_name, subj_info in SUBJECTS.items():
            if subject_lower == subj_name or subject_lower in subj_info["aliases"]:
                self.current_subject = subj_name
                print(f"✅ Предмет установлен: {subj_name}")
                return True
        
        print(f"❌ Предмет не найден: {subject}")
        print(f"   Доступные предметы: {', '.join(SUBJECTS.keys())}")
        return False
    
    def set_grade(self, grade: int) -> bool:
        """
        Устанавливает класс ученика.
        
        Args:
            grade: Номер класса (9, 10 или 11)
            
        Returns:
            True если класс корректный
        """
        if grade in [9, 10, 11]:
            self.current_grade = grade
            print(f"✅ Класс установлен: {grade}")
            return True
        
        print(f"❌ Некорректный класс: {grade}")
        print("   Допустимые значения: 9, 10, 11")
        return False
    
    def set_difficulty(self, difficulty: str) -> bool:
        """
        Устанавливает уровень сложности.
        
        Args:
            difficulty: Уровень ('easy', 'medium', 'hard')
            
        Returns:
            True если уровень корректный
        """
        if difficulty in DIFFICULTY_LEVELS:
            self.current_difficulty = difficulty
            print(f"✅ Сложность установлена: {difficulty}")
            return True
        
        print(f"❌ Некорректный уровень: {difficulty}")
        print(f"   Допустимые значения: {', '.join(DIFFICULTY_LEVELS.keys())}")
        return False
    
    def get_settings(self) -> Dict:
        """Возвращает текущие настройки"""
        return {
            "subject": self.current_subject,
            "grade": self.current_grade,
            "difficulty": self.current_difficulty
        }
    
    # ==================== СЕССИИ ====================
    
    def add_to_history(self, role: str, content: str):
        """Добавляет сообщение в историю сессии"""
        self.session_history.append({
            "role": role,
            "content": content
        })
    
    def get_history(self) -> List[Dict]:
        """Возвращает историю текущей сессии"""
        return self.session_history
    
    def clear_history(self):
        """Очищает историю сессии"""
        self.session_history = []
        print("🗑️ История сессии очищена")
    
    def save_session(self, name: str) -> bool:
        """
        Сохраняет текущую сессию в файл.
        
        Args:
            name: Имя файла сессии
            
        Returns:
            True если сохранение успешно
        """
        # TODO: Реализация в Уроке 4
        print(f"[Заглушка] Сохранение сессии: {name}")
        return True
    
    def load_session(self, name: str) -> bool:
        """
        Загружает сессию из файла.
        
        Args:
            name: Имя файла сессии
            
        Returns:
            True если загрузка успешна
        """
        # TODO: Реализация в Уроке 4
        print(f"[Заглушка] Загрузка сессии: {name}")
        return True


# Тестирование базового класса
if __name__ == "__main__":
    # Создаём помощника
    bot = SchoolAssistant()
    
    print("\n" + "="*50)
    print("Тестирование настроек:")
    print("="*50)
    
    # Тестируем настройки
    bot.set_subject("физика")
    bot.set_grade(10)
    bot.set_difficulty("hard")
    
    print(f"\nТекущие настройки: {bot.get_settings()}")
    
    print("\n" + "="*50)
    print("Тестирование заглушек методов:")
    print("="*50)
    
    # Тестируем методы (пока заглушки)
    print(bot.explain_topic("закон Ома"))
    print(bot.generate_exam("электричество", "ege", 3))
    print(bot.create_summary("механика"))
```

### CLI-интерфейс (`main.py`)

```python
"""
Точка входа SchoolBot — интерфейс командной строки.
"""

import sys
from assistant import SchoolAssistant


def print_help():
    """Выводит справку по командам"""
    help_text = """
╔════════════════════════════════════════════════════════════════╗
║                    🎓 SchoolBot — Справка                       ║
╠════════════════════════════════════════════════════════════════╣
║  ОСНОВНЫЕ КОМАНДЫ:                                             ║
║  /explain [тема]     — объяснить тему простым языком           ║
║  /exam [тема]        — создать тестовые задания ОГЭ/ЕГЭ        ║
║  /summarize [тема]   — создать краткий конспект                ║
║  /tutor [предмет]    — режим репетитора (диалог)               ║
║                                                                 ║
║  НАСТРОЙКИ:                                                     ║
║  /subject [предмет]  — выбрать предмет                         ║
║  /grade [9/10/11]    — установить класс                        ║
║  /level [easy/medium/hard] — уровень сложности                 ║
║  /settings           — показать текущие настройки              ║
║                                                                 ║
║  СЕССИИ:                                                        ║
║  /history            — показать историю                        ║
║  /save [имя]         — сохранить сессию                        ║
║  /load [имя]         — загрузить сессию                        ║
║  /clear              — очистить историю                        ║
║                                                                 ║
║  ДРУГОЕ:                                                        ║
║  /help               — эта справка                             ║
║  /exit               — выход                                   ║
╚════════════════════════════════════════════════════════════════╝
"""
    print(help_text)


def print_welcome():
    """Приветственное сообщение"""
    welcome = """
╔════════════════════════════════════════════════════════════════╗
║                                                                 ║
║     🎓 Добро пожаловать в SchoolBot!                           ║
║                                                                 ║
║     Умный помощник для подготовки к ОГЭ и ЕГЭ                  ║
║                                                                 ║
║     Введите /help для списка команд                            ║
║                                                                 ║
╚════════════════════════════════════════════════════════════════╝
"""
    print(welcome)


def parse_command(user_input: str) -> tuple:
    """
    Разбирает команду пользователя.
    
    Returns:
        (command, args) — команда и аргументы
    """
    parts = user_input.strip().split(maxsplit=1)
    command = parts[0].lower() if parts else ""
    args = parts[1] if len(parts) > 1 else ""
    return command, args


def main():
    """Главный цикл программы"""
    print_welcome()
    
    # Создаём помощника
    assistant = SchoolAssistant()
    
    # Главный цикл
    while True:
        try:
            # Получаем ввод пользователя
            user_input = input("\n📝 Вы: ").strip()
            
            if not user_input:
                continue
            
            # Разбираем команду
            command, args = parse_command(user_input)
            
            # Обработка команд
            if command == "/exit" or command == "/quit":
                print("\n👋 До встречи! Удачи на экзаменах!")
                break
            
            elif command == "/help":
                print_help()
            
            elif command == "/settings":
                settings = assistant.get_settings()
                print(f"\n⚙️ Текущие настройки:")
                print(f"   Предмет: {settings['subject']}")
                print(f"   Класс: {settings['grade']}")
                print(f"   Сложность: {settings['difficulty']}")
            
            elif command == "/subject":
                if args:
                    assistant.set_subject(args)
                else:
                    print("❌ Укажите предмет: /subject математика")
            
            elif command == "/grade":
                if args:
                    try:
                        grade = int(args)
                        assistant.set_grade(grade)
                    except ValueError:
                        print("❌ Укажите номер класса: /grade 11")
                else:
                    print("❌ Укажите класс: /grade 11")
            
            elif command == "/level":
                if args:
                    assistant.set_difficulty(args)
                else:
                    print("❌ Укажите уровень: /level medium")
            
            elif command == "/explain":
                if args:
                    result = assistant.explain_topic(args)
                    print(f"\n📚 {result}")
                else:
                    print("❌ Укажите тему: /explain производная")
            
            elif command == "/exam":
                if args:
                    # Парсим аргументы: /exam тема [oge/ege] [количество]
                    exam_args = args.split()
                    topic = exam_args[0] if exam_args else ""
                    exam_type = "ege"  # по умолчанию
                    num_questions = 3
                    
                    for arg in exam_args[1:]:
                        if arg.lower() in ["oge", "ege", "огэ", "егэ"]:
                            exam_type = "oge" if arg.lower() in ["oge", "огэ"] else "ege"
                        elif arg.isdigit():
                            num_questions = int(arg)
                    
                    result = assistant.generate_exam(topic, exam_type, num_questions)
                    print(f"\n📝 {result}")
                else:
                    print("❌ Укажите тему: /exam производная ege 5")
            
            elif command == "/summarize":
                if args:
                    result = assistant.create_summary(args)
                    print(f"\n📋 {result}")
                else:
                    print("❌ Укажите тему: /summarize интегралы")
            
            elif command == "/tutor":
                subject = args if args else assistant.current_subject
                print(f"\n👨‍🏫 Режим репетитора по предмету: {subject}")
                print("   Задавайте вопросы или введите /exit для выхода")
                # TODO: Реализация диалогового режима в Уроке 2
            
            elif command == "/history":
                history = assistant.get_history()
                if history:
                    print("\n📜 История сессии:")
                    for i, msg in enumerate(history[-10:], 1):  # Последние 10
                        role = "Вы" if msg["role"] == "user" else "Бот"
                        print(f"   {i}. {role}: {msg['content'][:50]}...")
                else:
                    print("📜 История пуста")
            
            elif command == "/save":
                if args:
                    assistant.save_session(args)
                else:
                    print("❌ Укажите имя: /save моя_сессия")
            
            elif command == "/load":
                if args:
                    assistant.load_session(args)
                else:
                    print("❌ Укажите имя: /load моя_сессия")
            
            elif command == "/clear":
                assistant.clear_history()
            
            else:
                # Если это не команда, обрабатываем как сообщение
                if user_input.startswith("/"):
                    print(f"❌ Неизвестная команда: {command}")
                    print("   Введите /help для списка команд")
                else:
                    # Обрабатываем как вопрос репетитору
                    result = assistant.tutor_message(user_input)
                    print(f"\n🤖 {result}")
        
        except KeyboardInterrupt:
            print("\n\n👋 До встречи!")
            break
        except Exception as e:
            print(f"\n❌ Ошибка: {e}")
            print("   Попробуйте ещё раз или введите /help")


if __name__ == "__main__":
    main()
```

### Файл зависимостей (`requirements.txt`)

```
requests>=2.28.0
python-dotenv>=1.0.0
```

### Пример .env файла (`.env.example`)

```
# OpenRouter API ключ
# Получите на https://openrouter.ai/
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here
```

## 4. Тестирование базовой структуры

Давайте проверим, что наша структура работает. Создайте все файлы и запустите:

```bash
# Установите зависимости
pip install -r requirements.txt

# Скопируйте .env.example в .env и добавьте свой API ключ
cp .env.example .env

# Запустите приложение
python main.py
```

Вы должны увидеть приветствие и иметь возможность вводить команды. Пока все основные команды возвращают заглушки — мы реализуем их в следующих уроках.

### 🔍 Проверьте себя

Убедитесь, что:
1. Приложение запускается без ошибок
2. Команда `/help` показывает справку
3. Команды `/subject`, `/grade`, `/level` меняют настройки
4. Команда `/settings` показывает текущие настройки

## Практические задания

### 🟢 Базовый уровень

**Задание 1: Расширение списка предметов**
Добавьте в `config.py` ещё 2-3 предмета (например, география, литература). Убедитесь, что команда `/subject` их распознаёт.

**Задание 2: Валидация ввода**
Добавьте в класс `SchoolAssistant` метод `validate_topic(topic: str) -> bool`, который проверяет, что тема не пустая и содержит минимум 2 символа.

### 🟡 Средний уровень

**Задание 3: Улучшение CLI**
Добавьте в CLI:
- Команду `/subjects` — показывает список всех доступных предметов
- Команду `/levels` — показывает описание уровней сложности
- Цветной вывод (используйте ANSI-коды или библиотеку `colorama`)

**Задание 4: Промпты для предмета**
Создайте в `PromptLibrary` специализированные промпты для одного предмета (например, для математики с акцентом на пошаговое решение).

### 🔴 Продвинутый уровень

**Задание 5: Диаграмма архитектуры**
Нарисуйте более детальную диаграмму архитектуры, показав:
- Какие методы есть у каждого класса
- Какие данные передаются между компонентами
- Где происходит обращение к API

**Задание 6: Альтернативная архитектура**
Предложите альтернативную архитектуру системы (например, с использованием событийной модели или паттерна "команда"). Опишите плюсы и минусы.

## Контрольные вопросы

1. **Что такое паттерн "Фасад" и как он применяется в нашей архитектуре?**
   <details>
   <summary>Ответ</summary>
   Фасад — это паттерн, предоставляющий простой интерфейс к сложной системе. В нашей архитектуре класс SchoolAssistant является фасадом: он скрывает сложность работы с роутером, менеджером контекста, проверкой качества и предоставляет простые методы вроде explain_topic() и generate_exam().
   </details>

2. **Зачем разделять логику на отдельные модули (router.py, context.py и т.д.)?**
   <details>
   <summary>Ответ</summary>
   Модульная структура даёт: 1) Возможность разрабатывать и тестировать компоненты независимо; 2) Простоту замены реализации (например, другой роутер); 3) Лучшую читаемость и поддержку кода; 4) Возможность переиспользования в других проектах.
   </details>

3. **Какой компонент выбирает модель для запроса и по каким критериям?**
   <details>
   <summary>Ответ</summary>
   ModelRouter выбирает модель на основе: типа задачи (объяснение, генерация тестов), сложности запроса, доступности модели, стоимости. Для простых задач выбирается быстрая дешёвая модель, для сложных — мощная.
   </details>

4. **Как PromptLibrary помогает организовать работу с промптами?**
   <details>
   <summary>Ответ</summary>
   PromptLibrary централизует все шаблоны промптов в одном месте, предоставляет методы для их форматирования с параметрами (предмет, класс, сложность), упрощает переиспользование и модификацию промптов без изменения основной логики.
   </details>

5. **Почему важно добавить систему проверки качества объяснений?**
   <details>
   <summary>Ответ</summary>
   LLM могут генерировать слишком сложные или неточные объяснения. QualityChecker с помощью LLM-as-a-judge оценивает понятность, полноту и соответствие уровню ученика. Это позволяет автоматически улучшать ответы до отправки пользователю.
   </details>

## Заключение урока

### Что мы изучили

В этом уроке мы заложили фундамент проекта SchoolBot:

- **Анализ требований** — определили, что нужно школьникам для подготовки к экзаменам
- **Архитектура системы** — спроектировали модульную структуру с чёткими обязанностями компонентов
- **Структура проекта** — создали файлы и базовые классы
- **CLI-интерфейс** — реализовали командную оболочку для взаимодействия

### Связь с предыдущими модулями

Мы уже используем знания из модулей 1-2:
- Понимание работы API (модуль 1)
- Структура промптов (модуль 1)
- Концепция роутинга моделей (модуль 2)
- Идея LLM-as-a-judge (модуль 2)

### Что нас ждёт дальше

В следующем уроке **"Реализация ядра системы"** мы:
- Создадим работающий ModelRouter
- Реализуем ContextManager
- Интегрируем PromptLibrary с реальными запросами к API
- Сделаем так, чтобы `/explain` и `/summarize` работали по-настоящему

### Ваш прогресс

🎉 **Отличное начало!** Вы создали каркас реального AI-приложения. Архитектура, которую мы спроектировали, масштабируема и позволит легко добавлять новые функции.

**Готовы оживить нашего помощника?** Переходите к [Уроку 2: Реализация ядра системы](lesson_2_core_implementation.md)!

---

## Дополнительные материалы

### Архитектура и паттерны:
- [Clean Architecture — Robert Martin](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
- [Python Project Structure](https://docs.python-guide.org/writing/structure/)
- [Design Patterns in Python](https://refactoring.guru/design-patterns/python)

### CLI-разработка:
- [Click — библиотека для CLI](https://click.palletsprojects.com/)
- [Rich — красивый вывод в терминале](https://rich.readthedocs.io/)
- [Typer — современный CLI-фреймворк](https://typer.tiangolo.com/)

### Образовательные ресурсы:
- [ФИПИ — демоверсии экзаменов](https://fipi.ru/)
- [Решу ЕГЭ](https://ege.sdamgia.ru/)
- [Решу ОГЭ](https://oge.sdamgia.ru/)

